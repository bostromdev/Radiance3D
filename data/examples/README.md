# Example datasets

Every example must state whether it is measured, simulated, imported, or processed
through `provenance.data_kind`. The `simulated/` examples exercise tools and formats;
they are not evidence about scanner performance or antenna behavior.
