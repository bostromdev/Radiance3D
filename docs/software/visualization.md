# Visualization

Planned outputs include polar cuts, spherical representations, interactive 3D views,
and dataset comparisons. No visualization is implemented.

A visualization must show units, frequency, data kind, normalization or calibration
state, and warnings. Interpolated regions must be distinguishable from samples.
Color scales and radial transformations must not imply absolute gain when the source
data is relative.
