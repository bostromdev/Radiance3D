# Development and data tools

Reusable dataset validators belong in `validation/`; explicit format translators
belong in `conversion/`. Tools must preserve provenance, units, and warnings and must
never overwrite raw input by default.
