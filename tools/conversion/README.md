# Conversion tools

No conversion tool is implemented. Future converters must document source semantics,
map units explicitly, set `provenance.data_kind` to `imported` or `processed`, and
retain source identifiers.
