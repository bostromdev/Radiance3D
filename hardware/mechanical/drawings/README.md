# Mechanical drawings

No released drawings exist. Future drawings must identify units, tolerances, material,
revision, and the matching source model.

Units are millimetres, matching [`Measurements/`](../../../Measurements/README.md).
A drawing must not introduce a dimension that has no measured or explicitly
design-chosen origin; where a dimension comes from a measurement, cite the component
file and the source picture.
