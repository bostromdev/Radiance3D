# Shared firmware components

This location is reserved for reusable, tested components shared by future firmware
targets. Code belongs here only after a second consumer exists; the protocol currently
lives with the controller to avoid a premature abstraction.
